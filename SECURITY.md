# 安全策略

## 报告安全漏洞

**请不要通过公开 Issue 报告安全问题。**

请使用 GitHub 的私密报告渠道：

👉 https://github.com/JacksenHu/workbuddy-manager/security/advisories/new

报告时请尽量包含：

- 漏洞类型与影响范围（例如：伪造请求头绕过 IP 白名单）
- 复现步骤或最小复现请求
- 受影响的版本
- 若能提供，附上修复建议

我会在收到报告后尽快确认，并在修复发布后致谢（若你愿意署名）。

## 支持范围

本项目处于活跃开发阶段，安全修复只针对**最新版本**发布。
建议部署后保持一键更新到最新版本。

## 部署者须知

本项目会接触腾讯账号凭据与网关密钥，部署时请务必遵守：

1. **必须经 HTTPS 访问**。会话 Cookie 与登录密码若无 TLS 保护可被中间人窃取。
2. **改掉初始管理员密码**，不要沿用部署脚本打印的默认值。
3. **端口只监听 127.0.0.1**（7863 / 7864），由反向代理对外提供服务。
4. 若前置 CDN，请正确设置 `WB_TRUSTED_PROXY_HOPS`，否则真实 IP 解析会出错，
   IP 白/黑名单与登录锁定将形同虚设。
5. **不要提交** `users.json`、`data/*.db`、`.env`、账号授权文件 ——
   这些已在 `.gitignore` 中排除，但请勿手动移除规则。

## 已知的安全设计

- 网关密钥仅存 SHA-256 哈希，明文只在创建时返回一次
- 管理端密码使用 PBKDF2-SHA256（26 万次迭代）加盐存储
- 会话使用 HttpOnly + SameSite=Lax 签名 Cookie，生产环境自动启用 `Secure`
- 真实 IP 取自反代覆盖写入的 `X-Real-IP`（`X-Forwarded-For` 首段可伪造），
  避免 IP 管控与登录锁定被冒充绕过
- 登录失败按 IP + 用户名双维度锁定
- 上游配置接口只下发凭据掩码，不外泄明文
- 所有文件操作做路径穿越校验
- 生产环境默认关闭 `/docs`、`/openapi.json`

完整审查结论见 [安全审查报告](docs/SECURITY-AUDIT.md)。
